My vim config and plugins etc.

When cloning to a new vim install, don't forget to add:
runtime .vimrc to ~/.vimrc

Run install.py in bundle/YouCompleteMe

Install https://github.com/powerline/fonts for glyphs in airline and nerdtree
